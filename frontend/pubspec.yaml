name: xrbone_app
description: "XRBone - AI-powered Medical Diagnostics Platform for X-ray Analysis"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI Components
  cupertino_icons: ^1.0.8

  # Database
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0+1
  path: ^1.9.0

  # Image Handling
  image_picker: ^1.1.2
  file_picker: ^10.3.7

  # Date Formatting
  intl: ^0.20.2

  # NEW: HTTP client for calling Python backend
  http: ^1.2.0
  window_manager: ^0.5.1
  xml: ^6.5.0
  url_launcher: ^6.3.1
  pdf: ^3.11.1
  printing: ^5.13.4
  path_provider: ^2.1.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true