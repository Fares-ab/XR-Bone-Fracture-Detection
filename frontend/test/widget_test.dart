import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test', (WidgetTester tester) async {
    // App replaced with XRBoneApp, skip the default counter test.
    expect(1, 1);
  });
}
